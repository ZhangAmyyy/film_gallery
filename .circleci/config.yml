version: 2.1

jobs:
  build_and_deploy:
    docker:
      - image: circleci/python:3.8
    
    environment:
      GCLOUD_PROJECT_ID: cinema-389321  
      GCLOUD_SERVICE_KEY: "aa0f40b698197db50b5e02bc714471a2994a9efa"

    steps:
      - checkout

      # 安装 gcloud SDK
      - run:
          name: Install gcloud SDK
          command: |
            curl https://sdk.cloud.google.com > install.sh
            bash install.sh --disable-prompts
            source '/home/circleci/google-cloud-sdk/completion.bash.inc'
            source '/home/circleci/google-cloud-sdk/path.bash.inc'
            gcloud version
          # persist_to_workspace:
          #   root: .
          #   paths:
          #     - $BASH_ENV
            
      # 配置 gcloud SDK
      - run:
          name: Configure gcloud SDK
          command: |
            source '/home/circleci/google-cloud-sdk/completion.bash.inc'
            source '/home/circleci/google-cloud-sdk/path.bash.inc'
            echo $GCLOUD_SERVICE_KEY
            echo $GCLOUD_SERVICE_KEY | base64 > testfile.txt  
            base64 -d testfile.txt  > ${HOME}/gcloud-service-key.json
            gcloud auth activate-service-account --key-file=${HOME}/gcloud-service-key.json
            gcloud config set project $GCLOUD_PROJECT_ID
            

workflows:
  build_and_deploy:
    jobs:
      - build_and_deploy
      